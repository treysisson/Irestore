# Claude Code — Start Here

This file exists so you can find this workspace on your local machine.

## Location
This folder is at: `~/Dropbox/Trey Sisson/iRestore/`
(Based on Finder breadcrumb: Dropbox > Trey Sisson > iRestore)

## To start Claude Code
```bash
cd ~/Dropbox/Trey\ Sisson/iRestore
claude
```

## What's here
- `.claude/CLAUDE.md` — Main instructions file (Claude reads this automatically)
- `.claude/docs/` — Reference docs (architecture, file inventory, interview intel)
- `irestore-prep/` — Main webapp build system (also a git repo)
- `irestore-prep-shareable/` — Shareable version for Kevin
- `interview_prep.html` — The compiled webapp

## Git repo
The git repo is at `irestore-prep/` (not the root). To push changes:
```bash
cd ~/Dropbox/Trey\ Sisson/iRestore/irestore-prep
git add . && git commit -m "your message" && git push
```
